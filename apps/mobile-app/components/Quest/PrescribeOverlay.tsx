/**
 * PrescribeOverlay — re-exports CardOverlay for prescribe quest flow.
 * This is now just an alias so existing imports don't break.
 */
export { default } from "../Itinerary/CardOverlay";
export type { CardOverlayProps as PrescribeOverlayProps } from "../Itinerary/CardOverlay";
